---
"changesets-gitea-action": patch
---

Create or update version pull request branches with one atomic commit based on the target branch, replacing the previous release commit on repeated runs instead of appending per-file and empty commits.
